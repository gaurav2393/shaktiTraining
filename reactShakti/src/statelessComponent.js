import React from 'react';

// const ListItem = (props) => (
//     <li>{props.value}</li>
// )

/*class List extends React.Component {
    render() {
        return (
            <ul>
                {this.props.items.map(item => <ListItem key={item.toString()} value={item} />)}
            </ul>
        )
    }
}*/

/*const List = ({items}) => (
    <ul>
        {items.map(item => <ListItem key={item.toString()} value={item} />)}
    </ul>
)
export default List;*/